{
  "buildCommand": "npm --workspace @doctor-tebar/shared run build && npm --workspace @doctor-tebar/web run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
